{
  "name": "uaus-frontend-office",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "apps/admin",
    "packages/api-client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@workspace/admin",
    "build": "npm run build --workspace=@workspace/admin",
    "typecheck": "npm run typecheck --workspace=@workspace/admin"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
